/*
User - Id, Email, Name, Phone Number, Year, Department, EventName, TransactionId 
*/

export interface User {
  id: string;
  email: string;
  name: string;
  phoneNumber: string;
  year: number;
  department: string;
  eventName: string;
  transactionId: string;
}
